export default function convertir(peso, conversión){

if (conversión == "libras")
{
  cálculo = (peso * libra)

} else {

 cálculo = (peso * kilo)

}

     console.log(cálculo)
}

const libra = 0.45359237;
const kilo = 2.20462;